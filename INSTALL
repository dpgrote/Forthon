
Requires python versions 2.2 or higher and the Numeric (numpy) package, which
can be obtained from sourceforge.net.

To install,

python setup.py install

Write permission is required for the python library site-packages directory,
which is usually located in /usr/lib/python2.x or /usr/local/lib/python2.x,
where x depends on the version.

See the examples, which act as the documentation.
